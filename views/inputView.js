const addButton = document.getElementById('button-add');
const nameInput = document.getElementById('input-name');
const backButton = document.getElementById('button-retry');

export { addButton, nameInput, backButton };
